#Usando virtualenv (NO NECESARIO)
pip install --upgrade virtualenv
Crear el virtualenv: virtualenv --system-site-packages DIRECTORIO (p.e. ~/.virtualenv/tensorflow)
Activar el virtualenv: source DIRECTORIO/bin/activate
Si no está instalado ya: easy_install -U pip
Para salir del virtualenv: deactivate

#INSTALL TENSORLOW
pip install --upgrade tensorflow

#INSTALL KERAS
Data serialization parser¿?: pip install pyyaml
For saving Keras models to disk: pip install h5py

pip install keras


#Librerias necesarias para ejecutar la práctica
pip install Pillow
pip install pydot
Mac:brew install graphviz
Linux: sudo apt-get install graphviz (¿?)
Maybe not necessary after previous command? pip install graphviz
